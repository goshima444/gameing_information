<div class="container">
  <div class="row my-4 ml-5">
    <h4 class="bg-light text-center col-3">タグ一覧・追加</h4>
  </div>


<%= form_with model: @tag, url: admin_tags_path do |f| %>
<div class="d-flex justify-content-between align-items-center col-6">
<p><%= f.label :"タグ名" %></p>
<P><%= f.text_field :name %></P>
<P><%= f.submit '新規登録', class: "btn btn-success btn-lg" %></P>
</div>
<% end %>
<table class="table col-4 mt-4">
  <thead class="bg-light">
      <th colspan="2">ジャンル</tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
    <tr>
      <td><%= tag.name %></td>
      <td>
        <%= link_to "編集する", edit_admin_tag_path(tag.id), class: "btn btn-success" %>
      </td>
    </tr>
    <% end %>
  </tbody>

</table>
</div>

